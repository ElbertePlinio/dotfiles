Use the browser tools and browser skill available in the current session for interactive page work. Use a purpose-built connector or documented API for supported data operations. For browser acceptance testing, follow the device-pass skill at skills/device-pass/SKILL.md.

A permitted isolated browser is a valid option when no extension is connected. Check which session and authentication it has before relying on it. Respect the current task's authorization and keep credentials, cookies, and captured private data out of logs and commits. A missing tool or session should produce a specific limitation, not an unconditional stop when another permitted route works.

Keep original screenshots as evidence. Choose cropping, zooming, or resizing for the task and active vision tool; see desktop-capture.md for machine capture details.
