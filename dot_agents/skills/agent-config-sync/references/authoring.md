Check existing skills before adding one; improve an existing skill rather than creating an overlapping sibling. Descriptions must state concrete triggers so a reader can choose without loading the body. Define observable completion and keep deeper material in linked references.

Remove unusable steps, retired tools, and repeated global policy. Distribute only to harnesses with the required tools. Validate frontmatter and relative references, and record body byte counts before and after material rewrites. Adapted third-party skills need license and origin metadata.

Canonical source is `dot_agents/skills/`, rendered to `~/.agents/skills/`. For an encrypted skill, use `chezmoi edit ~/.agents/skills/<name>/SKILL.md`. Add new sensitive content with `chezmoi add --encrypt`, never as plaintext source. Update `dot_agents/skill-targets.json` and compatible managed links only when distribution changes are authorized.
