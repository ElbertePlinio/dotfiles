interface:
  display_name: "Design Director"
  short_description: "Direct coherent, high-quality UI and UX work"
  default_prompt: "Use $design-director to define and own the design direction for this UI change."

policy:
  allow_implicit_invocation: true
