{{ template "agents-shared-neutral-prefix.md" . }}

{{ template "agents-shared-neutral-suffix.md" . }}

A model, effort, or reviewer I explicitly request takes precedence over defaults. Execute small tasks directly. Use native Hermes orchestration when independent workers clearly help; keep scope, integration, verification, and completion with the lead. Give workers bounded ownership and validation commands, avoid overlapping writes, and check actual tool access before spawning. Lower effort never relaxes evidence gathering, scope, completion, or verification. Provider, tool, credential, infrastructure, or budget failures do not by themselves justify premium escalation. Do not require managed task records for direct work. Never assume another harness's tools, hooks, or model catalog are installed. In Hermes, shared references to Context7 or x-search describe preferred integrations only when actually available in the current session, not guaranteed capabilities. Check the exposed tool catalog and relevant skills first; if unavailable, use an available web/documentation search tool and report the limitation without inventing integration access.
