# Codex

{{ template "agents-shared.md" . }}

Use Pickforge Lanes through MCP. Set taskKind, mode, model, and effort explicitly. Reviews use read-only; missing test execution uses a writable worker. Collect with lanes_wait before reporting completion and verify before lanes_assess. Codex uses explicit waits and Stop reminders, not Pi automatic continuation. Use lanes_abandon to stop lanes.
