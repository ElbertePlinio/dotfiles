#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd -P)
safety_dir=$(CDPATH= cd -- "$script_dir/.." && pwd -P)
if [ -f "$safety_dir/use-isolated-gh" ]; then
  export GH_CONFIG_DIR="$safety_dir/gh"
fi
exec "$safety_dir/real-gh" auth git-credential "$@"
