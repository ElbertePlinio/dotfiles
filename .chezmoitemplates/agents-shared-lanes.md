A model, effort, or reviewer I explicitly request takes precedence over catalog selection. Otherwise use lanes_models, or pickforge-lanes models when that tool is unavailable, for model choices, effort, named profiles, and review routing; treat it as the catalog reference rather than duplicating model details in instructions. Consider the whole eligible pool, not just the parent provider. Sol and Fable are the preferred Pi and Claude Code orchestrators. Sol and Opus 5.5 are the routine implementation, test, and integration pool: Sol for well-specified work, investigation, bounded reasoning, general engineering, backend work, and integration, and Opus for general and visual engineering. Opus has practical budget pressure at or below Sol because it is cheaper per task than Fable and goes further within Claude Code limits. Reserve Astra primarily for independent final review, difficult correctness work, backend invariants, concurrency, persistence, security, data-loss risk, and explicit high-reliability lead use. Astra may author when a reason-gated profile, required capability, or verified risk justifies it, but it is not the routine author or default lead. Fable is a selective first-class planner and advisor for consequential ambiguity, architecture, product intent, visual judgment, and nuanced review; it normally authors code only when those strengths are central or a suitable author demonstrably misses the intent. When delegating read-only planning or investigation, use model-backed assessment attempts, never an authoring kind disguised as assessment. Optimize cost-benefit through an accepted result, not minimum effort or author cost. Treat the catalog's Budget pressure score as a practical API or subscription resource prior, not literal API pricing; keep it separate from raw rates and unmeasured allowance percentages. Choose by required capabilities and verified results on similar tasks, then total cost including review, retries, rework, lead intervention, and budget pressure; no quotas, forced variety, or guessed allowance balance. Apply the catalog's task-specific effort guidance using ambiguity and verification strength, not task labels or diff size alone. Lower effort never relaxes evidence gathering, scope, completion, or verification requirements. Do not invent an automatic token cap. Set maxTokens only when explicitly requested; token accounting and elapsed, cost, repair, and concurrency limits stay in force. Give a brief task-specific reason for model and effort. Before spawning, check tool access, credential availability without exposing secrets, and execution mode. Read-only Pi and Claude Code lanes lack shell access; shell-based evidence gathering needs a workspace-write lane with explicit no-edit limits, a read-only Codex sandbox, or parent-run verification. Compare similar tasks across harnesses, including native agents and excluding diagnostics; separate model failures from tool failures. Preserve quality while reducing total cost, latency, or budget pressure rather than optimizing model diversity alone.

The current model executes the work directly by default, whatever its size. Discussion, investigation, and PR review get no automatic delegation and no review of the review. Planning is separate from delegation: plan when the work needs it, whoever executes. For larger multi-part implementation, write a short work plan and assign model ownership only where separate workers clearly help. Do not create a managed Pickforge Lanes task just to record a choice to execute directly. When delegating, keep scope, architecture, integration, and completion with the lead. Give workers owned files, required behavior, relevant examples, scope limits, and validation commands, route them through named profiles, and avoid overlapping writes. Workers own coherent implementation plus their tests and integration; the parent avoids duplicate exploration and unnecessary procedural turns. With Sol leading in Pi or Fable leading in Claude Code, offload delegated execution by task fit rather than defaulting to the parent model. Use Astra as a lead only when explicit extra reliability or a defined critical risk justifies its higher budget pressure. Requirements, checks, and reviews refer to the actual current source, not to a description of it. Send blocking findings in delegated work back to the original worker session; resume a suitable worker for focused repairs. Escalate effort for verified omissions, misunderstood interactions, or meaningful check failures, or choose it upfront for specific difficult reasoning or risk. Provider, tool, credential, infrastructure, or budget failures do not by themselves justify premium escalation. Code being shipped gets one independent review by a model that did not author it. Choose Fable for frontend, visual judgment, architecture intent, and ambiguous product behavior; choose Astra for backend, databases, performance, correctness, unnecessary complexity, and failure paths. Add both in separate reviews only for an explicitly sensitive risk such as security, auth, permissions, migrations, persistence, concurrency, or data loss. Different angles are useful when risk warrants them, but do not add reviewers through blind round-robin routing. Reuse evidence from unchanged revisions, recheck only what fixes affected, and do not spin repair or re-review rounds for optional polish. For managed tasks, verify settled work before recording acceptance, then record a compact observation of what the model actually did. Distinguish model defects from environment, auth, or interruption failures, and never invent scores or quality guarantees; passing checks and a model review raise confidence, they do not establish correctness. Task records survive compaction, and the generated task Markdown is a view rather than the authority or an editable source.

Always use openai-codex/gpt-6-astra at low effort to execute device-pass and Picklab/Pickforge computer-use tasks. This overrides other model and effort defaults. Other models may lead or implement fixes, but must hand off this execution to a tool-enabled Astra low session. Pi lanes disable extensions and do not inherit browser/MCP tools. If the required session or tools are unavailable, report the blocker instead of silently substituting.
