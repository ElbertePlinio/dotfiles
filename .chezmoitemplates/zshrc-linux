# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# Omarchy environment (OMARCHY_PATH + PATH), shared with Bash/login shells.
[[ -r /usr/share/omarchy/default/bash/env-bootstrap ]] && source /usr/share/omarchy/default/bash/env-bootstrap

# Legacy CachyOS zsh defaults, only when this dotfile is used on an old CachyOS host.
[[ -r /usr/share/cachyos-zsh-config/cachyos-config.zsh ]] && source /usr/share/cachyos-zsh-config/cachyos-config.zsh

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

export PATH="$HOME/fvm/bin:$PATH"
export PATH="$HOME/ProgramsDev/flutter/bin:$PATH"
export PATH="$HOME/.cargo/bin:$PATH"
export PATH="$HOME/.pub-cache/bin:$PATH"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
export PATH="$HOME/.local/bin:$PATH"

# pi-caffeinate: idle-only inhibit (sleep inhibit triggers polkit TTY auth and steals Pi input)
if [[ -z "${PI_CAFFEINATE_COMMAND:-}" ]]; then
  export PI_CAFFEINATE_COMMAND='systemd-inhibit --what=idle --who=pi-caffeinate --why="Pi agent is running" --mode=block sleep infinity'
fi

# pnpm
export PNPM_HOME="$HOME/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end

# bun completions
[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"
export CHROME_EXECUTABLE=/usr/bin/google-chrome-stable

{{ template "zshrc-common" . -}}
# Qwen3.6 35B A3B APEX - RX 9070 XT - 262K TurboQuant
qwen262k() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/qwen3.6-apex/Qwen3.6-35B-A3B-APEX-I-Compact.gguf"
  local MMPROJ="$HOME/models/qwen3.6-apex/mmproj.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --mmproj "$MMPROJ" \
    --alias qwen3.6-35b-a3b-apex-262k-rx9070xt-turbo3-coding \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    -ngl 999 \
    --ctx-size 262144 \
    -fa on \
    --cache-type-k turbo3 \
    --cache-type-v turbo3 \
    --n-cpu-moe 22 \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 512 \
    --ubatch-size 256 \
    --parallel 1 \
    --cache-reuse 256 \
    --metrics \
    --temp 0.3 \
    --top-p 0.9 \
    --top-k 20 \
    --min-p 0.0 \
    --presence-penalty 0.0 \
    --repeat-penalty 1.05
}

qwen-hermes-fast() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/qwen3.6-apex/Qwen3.6-35B-A3B-APEX-I-Compact.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --alias qwen3.6-35b-a3b-apex-262k-rx9070xt-turbo3-coding \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    -ngl 99 \
    --ctx-size 65536 \
    -fa on \
    --cache-type-k turbo3 \
    --cache-type-v turbo3 \
    --n-cpu-moe 8 \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 1024 \
    --ubatch-size 512 \
    --parallel 1 \
    --cache-reuse 256 \
    --metrics \
    --temp 0.3 \
    --top-p 0.9 \
    --top-k 20 \
    --min-p 0.0 \
    --presence-penalty 0.0 \
    --repeat-penalty 1.05
}

# Qwen3.6 35B A3B APEX I-Mini 14GB - RX 9070 XT - 64K - n-cpu-moe 4 (daily / Claude Code, sobra VRAM p/ desktop)
qwen-mini() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/qwen3.6-apex/Qwen3.6-35B-A3B-APEX-I-Mini.gguf"
  local MMPROJ="$HOME/models/qwen3.6-apex/mmproj.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --mmproj "$MMPROJ" \
    --no-mmproj-offload \
    --alias qwen3.6-mini \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    -ngl 999 \
    --ctx-size 65536 \
    -fa on \
    --cache-type-k turbo3 \
    --cache-type-v turbo3 \
    --n-cpu-moe 4 \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 1024 \
    --ubatch-size 512 \
    --parallel 1 \
    --cache-reuse 256 \
    --metrics \
    --temp 0.3 \
    --top-p 0.9 \
    --top-k 20 \
    --min-p 0.0 \
    --presence-penalty 0.0 \
    --repeat-penalty 1.05
}

# Mesmo modelo, todos experts na GPU (n-cpu-moe 0) - so com apps de GPU fechados
qwen-mini-max() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/qwen3.6-apex/Qwen3.6-35B-A3B-APEX-I-Mini.gguf"
  local MMPROJ="$HOME/models/qwen3.6-apex/mmproj.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --mmproj "$MMPROJ" \
    --no-mmproj-offload \
    --alias qwen3.6-mini \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    -ngl 999 \
    --ctx-size 32768 \
    -fa on \
    --cache-type-k turbo3 \
    --cache-type-v turbo3 \
    --n-cpu-moe 0 \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 1024 \
    --ubatch-size 512 \
    --parallel 1 \
    --cache-reuse 256 \
    --metrics \
    --temp 0.3 \
    --top-p 0.9 \
    --top-k 20 \
    --min-p 0.0 \
    --presence-penalty 0.0 \
    --repeat-penalty 1.05
}

# Qwythos 9B local model - Fable/Mythos distilled model for Claude Code/Pi tests
qwythos() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/qwythos-9b/Qwythos-9B-Claude-Mythos-5-1M-Q4_K_M.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --alias qwythos \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    --chat-template-file "$HOME/.config/llama-cpp/qwythos-claude-code.jinja" \
    --chat-template-kwargs '{"enable_thinking":false}' \
    -ngl 999 \
    --ctx-size 229376 \
    -fa on \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 1024 \
    --ubatch-size 512 \
    --parallel 1 \
    --cache-reuse 256 \
    --metrics \
    --temp 0.6 \
    --top-p 0.95 \
    --top-k 20 \
    --repeat-penalty 1.05
}

# Fable traces meme model - tiny Qwen3 4B quant for quick local testing
fable-traces() {
  cd "$HOME/src/llama-cpp-turboquant" || return 1

  local MODEL="$HOME/models/fable-traces/fable-traces-q4_k_m.gguf"

  HIP_VISIBLE_DEVICES=0 ./build/bin/llama-server \
    -m "$MODEL" \
    --alias fable-traces \
    --host 127.0.0.1 \
    --port 8080 \
    --jinja \
    -ngl 999 \
    --ctx-size 32768 \
    -fa on \
    --threads 8 \
    --threads-batch 12 \
    --batch-size 1024 \
    --ubatch-size 512 \
    --parallel 1 \
    --metrics \
    --temp 0.6 \
    --top-p 0.95 \
    --top-k 20 \
    --repeat-penalty 1.05
}

stopllama() {
  pkill -f llama-server
}

export CLIPROXY_API_BASE_URL="http://127.0.0.1:8317/v1"

# Claude Code apontado pro Qwen local (I-Mini via cli-proxy-api)
claude-mini() {
  ANTHROPIC_BASE_URL="http://127.0.0.1:8317" \
  ANTHROPIC_AUTH_TOKEN="$CLIPROXY_API_KEY" \
  ANTHROPIC_MODEL="qwen-mini" \
  ANTHROPIC_SMALL_FAST_MODEL="qwen-mini" \
  claude "$@"
}

# Claude Code/Pi pointed at local Qwythos on llama-server :8080
claude-qwythos() {
  ANTHROPIC_BASE_URL="http://127.0.0.1:8317" \
  ANTHROPIC_AUTH_TOKEN="$CLIPROXY_API_KEY" \
  ANTHROPIC_MODEL="qwythos" \
  ANTHROPIC_SMALL_FAST_MODEL="qwythos" \
  claude "$@"
}

pi-qwythos() {
  pi --model local-llama/qwythos "$@"
}

# Claude Code/Pi pointed at local fable-traces on llama-server :8080
claude-fable-traces() {
  ANTHROPIC_BASE_URL="http://127.0.0.1:8317" \
  ANTHROPIC_AUTH_TOKEN="$CLIPROXY_API_KEY" \
  ANTHROPIC_MODEL="fable-traces" \
  ANTHROPIC_SMALL_FAST_MODEL="fable-traces" \
  claude "$@"
}

pi-fable-traces() {
  OPENAI_API_KEY="none" \
  OPENAI_BASE_URL="http://127.0.0.1:8080/v1" \
  pi --provider openai --model fable-traces "$@"
}

# Claude Code on the GPT-5.6 Codex subscription via cli-proxy-api.
claude-codex() {
  local model="${CLAUDE_CODEX_MODEL:-gpt-5.6-sol}"
  CLAUDE_CONFIG_DIR="$HOME/.claude" \
  ANTHROPIC_BASE_URL="http://127.0.0.1:8317" \
  ANTHROPIC_AUTH_TOKEN="$CLIPROXY_API_KEY" \
  ANTHROPIC_MODEL="$model" \
  ANTHROPIC_SMALL_FAST_MODEL="gpt-5.6-sol" \
    "$HOME/.local/bin/claude" --dangerously-skip-permissions "$@"
}
claude-sol()   { CLAUDE_CODEX_MODEL="gpt-5.6-sol"   claude-codex "$@"; }

claudex() {
  CLAUDE_CODE_ALWAYS_ENABLE_EFFORT=1 \
  CLAUDE_CODE_AUTO_COMPACT_WINDOW=180000 \
  ENABLE_TOOL_SEARCH=auto \
    claude-sol "$@"
}

# Start OMP in a Pickforge child repo while retaining workspace instructions.
ompp() {
  local repo="$HOME/Projects/Pickforge/${1:?project name required}"
  shift

  omp \
    --cwd "$repo" \
    --append-system-prompt "$HOME/Projects/Pickforge/AGENTS.md" \
    "$@"
}

# >>> grok installer >>>
export PATH="$HOME/.grok/bin:$PATH"
fpath=(~/.grok/completions/zsh $fpath)
autoload -Uz compinit && compinit -C
# <<< grok installer <<<
