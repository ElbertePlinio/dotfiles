## Shared Agent Memory

Before user-specific work, read relevant `{{ .chezmoi.homeDir }}/AgentMemory`: always `CORE_PROFILE.md`, `WRITING_STYLE.md`, `BOUNDARIES.md`; coding adds `WORK_AND_PROJECTS.md`; social/media adds `SOCIAL_MEDIA.md`; also relevant `projects/*.md`. Never import `archive_raw/` unless scoped. Never store secrets in memory.
